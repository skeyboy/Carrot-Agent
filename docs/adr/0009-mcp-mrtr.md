# ADR 0009: MCP MRTR

- 状态：P6 延后
- 决策日期：2026-08-20

## 决策

P6 不为 MCP Server 开启多轮工具推理（MRTR）。一次 MCP 工具调用只能产生一次受限结果，并返回现有 Agent Runtime。

## 理由与进入条件

MRTR 会在外部控制下扩大步骤数、上下文和副作用面。后续需要独立的轮次/费用预算、每轮工具策略、嵌套审批、防递归、完整事件日志与中途恢复模型，不能复用单次调用的隐式循环。
