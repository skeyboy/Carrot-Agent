# Linux shell placeholder

Linux packaging is not implemented in the current macOS-first milestone. A future Linux entry may place desktop integration, package metadata and IDE helpers here while continuing to build the shared `src-tauri` Cargo package. Tauri-specific overrides must use `src-tauri/tauri.linux.conf.json` so the CLI can merge them automatically.
